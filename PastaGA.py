import random

base_pop = 100
generations = 1000
pasta_book = {}
ingredient_list = []

def generate_basepop(pop_level, ingredient_list):
    for chrom in range(0, pop_level):

    return

def fitness_check(chromosome):
    #determines how delicious pasta is based off of genes(ingredients)
    return

generate_basepop(base_pop, ingredient_list)